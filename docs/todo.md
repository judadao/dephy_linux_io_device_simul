# TODO

- Add transport adapter API for MQTT, Modbus/TCP, and file sinks.
- Add script format documentation and richer scenario fixtures.
- Add demo integration for IO device + matrix + fast module evaluation.
- Decide whether this module should depend on `dephy_industrial_io` directly or
  keep a small Linux-only compatibility API.

